# Contributing

Keep gameplay changes small and testable.

Before committing:

1. Run `python3 tools/validate_levels.py`.
2. Build with CMake when SDL2 is available.
3. Keep new assets inside `assets/`.
4. Document new level characters or controls.
