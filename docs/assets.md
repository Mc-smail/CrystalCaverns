# Asset Notes

Sprite assets are stored as BMP files under `assets/sprites`.

The game loads textures by fixed relative paths, so keep the `assets/` directory next to the executable or use the CMake build that copies assets automatically.
